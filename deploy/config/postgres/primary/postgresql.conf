# PostgreSQL 主节点配置
listen_addresses = '*'
port = 5432

# WAL 复制配置
wal_level = replica
max_wal_senders = 3
wal_keep_size = 64MB
hot_standby = on

# 同步复制 (可选，注释掉则为异步复制)
# synchronous_standby_names = 'pg-replica'
