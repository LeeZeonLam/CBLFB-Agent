# Grafana 仪表盘配置
apiVersion: 1

providers:
  - name: 'FBA LogiAI Dashboards'
    orgId: 1
    folder: 'FBA LogiAI'
    folderUid: 'fba-logi'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
