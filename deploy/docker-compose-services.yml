version: '3.8'

services:
  # ==========================================
  # Nacos 服务注册与配置中心
  # ==========================================
  nacos:
    image: nacos/nacos-server:v2.3.0
    container_name: fba-nacos
    restart: always
    environment:
      MODE: standalone
      SPRING_DATASOURCE_PLATFORM: embedded
      JVM_XMS: 256m
      JVM_XMX: 512m
    ports:
      - "10848:8848"
      - "11848:9848"
    networks:
      - fba-net
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8848/nacos/actuator/health"]
      interval: 30s
      timeout: 10s
      retries: 5

  # ==========================================
  # API 网关服务
  # ==========================================
  fba-gateway:
    image: fba-logi-gateway:v1
    container_name: fba-gateway
    restart: always
    environment:
      NACOS_SERVER_ADDR: nacos:8848
      JAVA_OPTS: -Xms256m -Xmx512m
    ports:
      - "10080:8080"
    depends_on:
      nacos:
        condition: service_healthy
    networks:
      - fba-net
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/actuator/health"]
      interval: 30s
      timeout: 10s
      retries: 5

  # ==========================================
  # 聊天微服务
  # ==========================================
  fba-chat:
    image: fba-logi-chat:v1
    container_name: fba-chat
    restart: always
    environment:
      NACOS_SERVER_ADDR: nacos:8848
      DB_HOST: pg-primary
      DB_PORT: 5432
      DB_NAME: ${POSTGRES_DB:-fba_logi_db}
      DB_USER: ${POSTGRES_USER:-fba_admin}
      DB_PASSWORD: ${POSTGRES_PASSWORD:-fba_secret_pass}
      REDIS_HOST: redis-primary
      REDIS_PORT: 6379
      REDIS_PASSWORD: ${REDIS_PASSWORD:-redis_secret}
      DEEPSEEK_API_KEY: ${DEEPSEEK_API_KEY:-}
      ZHIPU_API_KEY: ${ZHIPU_API_KEY:-}
      LLM_PROVIDER: ${LLM_PROVIDER:-deepseek}
      JAVA_OPTS: -Xms512m -Xmx1g
    ports:
      - "10081:8081"
    depends_on:
      nacos:
        condition: service_healthy
    networks:
      - fba-net
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8081/actuator/health"]
      interval: 30s
      timeout: 10s
      retries: 5

  # ==========================================
  # 数据微服务
  # ==========================================
  fba-data:
    image: fba-logi-data:v1
    container_name: fba-data
    restart: always
    environment:
      NACOS_SERVER_ADDR: nacos:8848
      DB_HOST: pg-primary
      DB_PORT: 5432
      DB_NAME: ${POSTGRES_DB:-fba_logi_db}
      DB_USER: ${POSTGRES_USER:-fba_admin}
      DB_PASSWORD: ${POSTGRES_PASSWORD:-fba_secret_pass}
      REDIS_HOST: redis-primary
      REDIS_PORT: 6379
      REDIS_PASSWORD: ${REDIS_PASSWORD:-redis_secret}
      JAVA_OPTS: -Xms256m -Xmx512m
    ports:
      - "10082:8082"
    depends_on:
      nacos:
        condition: service_healthy
    networks:
      - fba-net
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8082/actuator/health"]
      interval: 30s
      timeout: 10s
      retries: 5

  # ==========================================
  # RAG 微服务
  # ==========================================
  fba-rag:
    image: fba-logi-rag:v1
    container_name: fba-rag
    restart: always
    environment:
      NACOS_SERVER_ADDR: nacos:8848
      MILVUS_HOST: milvus-standalone
      MILVUS_PORT: 19530
      ZHIPU_API_KEY: ${ZHIPU_API_KEY:-}
      JAVA_OPTS: -Xms256m -Xmx512m
    ports:
      - "10083:8083"
    depends_on:
      nacos:
        condition: service_healthy
    networks:
      - fba-net
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8083/actuator/health"]
      interval: 30s
      timeout: 10s
      retries: 5

networks:
  fba-net:
    external: true
